The first "page" that we work on should be basic chat. essentially a more polished version of `ugly-chat.py`: simple one-off questions that get answered super quickly. 


and an "in-line" PDF window which is easily collapsible.

## Retrieved Context UI Elements

Natural language (like the chunked markdown files) should be simple expandable cutouts or something like that. when you click on the text it moves the PDF window to the page where the file comes from

